# youtube_video_trimmer
A Python script to trim YouTube videos.

## Instructions  

1. Install ffmpeg
```
sudo apt update
sudo apt install ffmpeg
```

2. Install required Python packages
```
pip install -r requirements.txt
```

3. Run Python script
```
python main.py <youtube-url> <hh:mm (start)> <hh:mm (end)>
```
